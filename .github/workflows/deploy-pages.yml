name: Auf GitHub Pages veröffentlichen

# Die Pages-Seite wird unten von configure-pages selbst angelegt, sofern unter
# Settings → Actions → General die Workflow-Rechte auf "Read and write" stehen.
on:
  push:
    branches: [main]
  workflow_dispatch:
  # Nach jedem erfolgreichen Snapshot neu veröffentlichen, damit die frischen
  # Zahlen sofort online stehen.
  workflow_run:
    workflows: ['Stündlicher Snapshot']
    types: [completed]

permissions:
  contents: read
  pages: write
  id-token: write

# Läuft ein Deploy bereits, wartet das nächste, statt es abzubrechen.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    # Bei workflow_run nur veröffentlichen, wenn der Snapshot auch geklappt hat.
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Rechenlogik prüfen
        run: node scripts/test-calc.mjs

      - uses: actions/configure-pages@v5
        with:
          # Legt die Pages-Seite bei Bedarf selbst an (Build-Typ "workflow"),
          # statt sie in den Repo-Einstellungen vorauszusetzen. Ohne das
          # scheitert der Schritt mit "Get Pages site failed … Not Found".
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - id: deployment
        uses: actions/deploy-pages@v4
